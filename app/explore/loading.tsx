import { RouteLoadingState } from "@/components/ui/RouteLoadingState";

export default function ExploreLoading() {
  return <RouteLoadingState description="Preparando búsqueda, mapa y destinos de guardado." title="Cargando Explore" variant="map" />;
}
