import { RouteLoadingState } from "@/components/ui/RouteLoadingState";

export default function GroupDecisionsLoading() {
  return <RouteLoadingState description="Preparando las encuestas del grupo." title="Cargando encuestas" />;
}
