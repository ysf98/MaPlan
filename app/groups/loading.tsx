import { RouteLoadingState } from "@/components/ui/RouteLoadingState";

export default function GroupsLoading() {
  return <RouteLoadingState description="Preparando tus grupos y sus últimos datos." title="Cargando grupos" />;
}
