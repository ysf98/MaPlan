import { RouteLoadingState } from "@/components/ui/RouteLoadingState";

export default function DashboardLoading() {
  return <RouteLoadingState description="Actualizando tus grupos, invitaciones y actividad reciente." title="Cargando dashboard" />;
}
