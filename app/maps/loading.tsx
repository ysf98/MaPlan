import { RouteLoadingState } from "@/components/ui/RouteLoadingState";

export default function MapsLoading() {
  return <RouteLoadingState description="Preparando tus mapas disponibles." title="Cargando mapas" />;
}
